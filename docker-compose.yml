version: '3.8'

services:
  modeswap:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: modeswap-app
    ports:
      - "3000:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
